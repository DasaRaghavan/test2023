import random

lower_bound = int(input("Enter a lower bound: "))
upper_bound = int(input("Enter a upper bound: "))

my_random_output = random.randint(lower_bound, upper_bound)

print(my_random_output)
